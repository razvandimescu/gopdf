*.pdf
# Encryption fixtures are the test, not test input, so they are tracked.
!pdf/testdata/*.pdf
example_out
.DS_Store
docs/
BUGS.md

# Built command binaries (same name as their cmd/ directory)
/gopdf
/extract_tables
/inspect
/merge
/quote
/sample
/watermark
